# Token Service base URL. Dev default matches api/main.py's default uvicorn
# port (8000). Update before deployment to the real deployed Token Service URL.
VITE_TOKEN_SERVICE_URL=http://localhost:8000
